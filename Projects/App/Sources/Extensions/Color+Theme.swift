import SwiftUI

extension Color {
    // MARK: - Theme Colors

    /// Primary theme color - Dark blue (light) / #64B5F6 (dark)
    static let themePrimary = Color("themePrimary")

    /// Body text color - Navy-gray (light) / #E0E0E0 (dark)
    static let themeBodyText = Color("themeBodyText")

    /// Background color - Warm beige (light) / #121212 (dark)
    static let themeBackground = Color("themeBackground")

    /// Disabled background - Near-white (light) / #1E1E1E (dark)
    static let themeDisabledBackground = Color("themeDisabledBackground")

    /// Disabled border - Light gray (light) / #333333 (dark)
    static let themeDisabledBorder = Color("themeDisabledBorder")

    // MARK: - Additional Colors

    /// Alternative blue for quiz elements - Dark blue (light) / #4FC3F7 (dark)
    static let themeAlternativeBlue = Color("themeAlternativeBlue")

    /// Light blue for answer backgrounds - (light) / #64B5F6 (dark)
    static let themeLightBlue = Color("themeLightBlue")

    /// Brown color for splash screen
    static let themeBrown = Color("themeBrown")

    /// Splash background - Very light blue (light) / #0D1117 (dark)
    static let themeSplashBackground = Color("themeSplashBackground")

    /// Blue for splash elements - (light) / #64B5F6 (dark)
    static let themeSplashBlue = Color("themeSplashBlue")

    /// Tint color for enabled alarms - (light) / #4FC3F7 (dark)
    static let themeTint = Color("themeTint")

    /// Secondary text - Subdued hints/dates - 50% black (light) / #A0A0A0 (dark)
    static let themeSecondaryText = Color("themeSecondaryText")

    /// Surface / Card - Elevated background layer - warm near-white (light) / #252525 (dark)
    static let themeSurface = Color("themeSurface")
}
